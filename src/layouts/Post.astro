---
import Base from './Base.astro';
import PostSidebar from '../components/PostSidebar.astro'
import { formatDate } from '../wwwroot/js/utils';

const { frontmatter, previousPost, nextPost } = Astro.props;
const { title, description, date, category, author, image, tags } = frontmatter;
---

<Base pageTitle="Post">
    <section class="post-wrapper">
        <div class="post">
            <div class="post-hero">
                <h1>{title}</h1>
                <div class="post-metadata">
                    <span class="post-metadata-date">{formatDate(date)}</span>
                    <div class="post-metadata-tags">Categories: {tags.join(', ')}</div>
                </div>
            </div>

            <div class="post-content">
                <div>
                    <slot />
                </div>

                <div class="post-navigation">
                    {previousPost ? (
                            <a href={previousPost.id} class='with-arrow left'>
                                <span class="tooltip">{previousPost.title}</span>
                                <span>Previous</span>
                            </a>
                    ) : (
                            <span class="disabled">Previous</span>
                    )}

                    {nextPost ? (
                            <a href={nextPost.id} class='with-arrow right'>
                                <span class="tooltip">{nextPost.title}</span>
                                <span>Next</span>
                            </a>
                    ) : (
                            <span class="disabled">Next</span>
                    )}
                </div>
            </div>
        </div>
        <PostSidebar></PostSidebar>
    </section>
</Base>


